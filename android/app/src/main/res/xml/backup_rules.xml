<?xml version="1.0" encoding="utf-8"?>
<!--
  Hermex backup rules — Android 11 and lower.

  App-private data is intentionally excluded from backup. Whole domains are
  denied so credentials, Room databases and sidecars, preferences, and future
  private files cannot become eligible through a filename change.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
